class AccentDialog(Dialog) {
    this.label = "Insert Accent";
    this.idname = "insert-accent-dialog";
    this.options = {
        closeDialog = true, // Show the "X" thing on the dialog
        toggleVisibility = true
    };

    const layout = {
        let 
    }
}